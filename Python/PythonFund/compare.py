list_one = ['celery','carrots','bread','milk']
list_two = ['celery','carrots','bread','cream']
#
#define a function
#check if length is same
#iterate thru array 1 index and match its value to array 2s value.
#if same, print out 'this lists are the same', or else print 'the lists are not the same'

def compare(x,y):
    if len(x) != len(y):
        print "the lists are not the same";
        return

    if x[i][i] != y[i][i]
    for i in range(0,len(x)-1):
        if x[i] != y[i]:
            print "the lists are not the same"
            break
        else:
            print "the lists are the same."
            break


compare(list_one,list_two)
